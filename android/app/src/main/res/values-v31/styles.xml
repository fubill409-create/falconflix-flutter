<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ 系统启动屏：底色用暗底，去掉白闪，无缝接 Flutter 金鹰 splash。 -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/splash_bg</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/splash_bg</item>
        <!-- 透明图标 = 系统启动屏不显示小鹰头，开机只剩 Flutter 金鹰大动画。 -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon_blank</item>
    </style>
    <!-- flutter_stripe PaymentSheet 需要宿主 Activity 用 MaterialComponents 主题 -->
    <style name="NormalTheme" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
